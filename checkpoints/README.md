# Checkpoints

Place the six released checkpoints here:

    patchex_opt_s0.pt  patchex_opt_s1.pt  patchex_opt_s2.pt   # optimal temperature
    patchex_ph_s0.pt   patchex_ph_s1.pt   patchex_ph_s2.pt    # optimal pH

Each is ~75 MB (fp16, auxiliary direct_head stripped). Set PATCHEX_CHECKPOINTS
to point elsewhere if you prefer.
